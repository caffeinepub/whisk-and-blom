import { useQuery } from '@tanstack/react-query';
import { useActor } from './useActor';

// This file is intentionally minimal as we're using feature-specific hooks
// All queries are organized in their respective feature hooks (useProducts, useCart, etc.)
